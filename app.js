document.addEventListener("DOMContentLoaded", function () { /* ========================= BUSINESS ========================= */ const params = new URLSearchParams( window.location.search ); const businessId = params.get("business") || "realisticportraitartist"; const businessFolder = "businesses/" + businessId + "/"; const configUrl = businessFolder + "config/data.json"; let businessData = null; let galleryImages = []; let currentImage = 0; /* ========================= HELPERS ========================= */ function $(id) { return document.getElementById(id); } function resolvePath(path) { if (!path) { return ""; } path = String(path); if ( path.startsWith("http://") || path.startsWith("https://") || path.startsWith("//") || path.startsWith("/") ) { return path; } return businessFolder + path; } function formatPrice(price) { if ( price === undefined || price === null || price === "" ) { return ""; } if (typeof price === "number") { return "₹" + price.toLocaleString("en-IN"); } return String(price); } /* ========================= LOAD DATA ========================= */ fetch(configUrl) .then(function (response) { if (!response.ok) { throw new Error( "Unable to load business data: " + configUrl ); } return response.json(); }) .then(function (data) { businessData = data; renderBusiness(); }) .catch(function (error) { console.error(error); $("businessName").textContent = "Unable to load business"; $("description").textContent = error.message; }); /* ========================= MAIN RENDER ========================= */ function renderBusiness() { const business = businessData.business || {}; document.title = business.name || "Sarvyanta"; $("businessName").textContent = business.name || ""; $("tagline").textContent = business.tagline || ""; $("description").textContent = business.description || ""; /* LOGO */ if (business.logo) { const logo = $("businessLogo"); logo.src = resolvePath( business.logo ); logo.style.display = "block"; } /* SECTIONS */ const sections = businessData.sections || {}; if (sections.about === false) { $("about").style.display = "none"; } if (sections.services === false) { $("services").style.display = "none"; } else { renderServices(); } if (sections.products === false) { $("products").style.display = "none"; } else { renderProducts(); renderNotes(); } if (sections.gallery === false) { $("gallery").style.display = "none"; } else { renderGallery(); } if (sections.contact === false) { $("contact").style.display = "none"; } else { renderContact(); } renderSocial(); } /* ========================= SERVICES ========================= */ function renderServices() { const container = $("servicesList"); container.innerHTML = ""; const services = businessData.services || []; if ( !Array.isArray(services) || services.length === 0 ) { return; } services.forEach( function (service) { const item = document.createElement("div"); item.className = "service-item"; const title = document.createElement("h3"); title.textContent = service; item.appendChild(title); /* WhatsApp */ const whatsapp = createWhatsAppButton( "Hi, I am interested in your service: " + service, "service-whatsapp" ); if (whatsapp) { item.appendChild( whatsapp ); } container.appendChild( item ); } ); } /* ========================= PRODUCTS ========================= */ function renderProducts() { const container = $("productsList"); container.innerHTML = ""; const products = businessData.products || []; if ( !Array.isArray(products) || products.length === 0 ) { return; } products.forEach( function (product) { const card = document.createElement("div"); card.className = "product-card"; const title = document.createElement("h3"); title.textContent = product.name || ""; card.appendChild(title); if ( product.price !== undefined ) { const price = document.createElement("div"); price.className = "product-price"; price.textContent = formatPrice( product.price ); card.appendChild(price); } /* WhatsApp */ const whatsapp = createWhatsAppButton( "Hi, I am interested in: " + product.name ); if (whatsapp) { card.appendChild( whatsapp ); } container.appendChild( card ); } ); } /* ========================= NOTES ========================= */ function renderNotes() { const container = $("productsNotes"); container.innerHTML = ""; const notes = businessData.productsNotes || []; if ( !Array.isArray(notes) || notes.length === 0 ) { return; } const wrapper = document.createElement("div"); wrapper.className = "notes"; const title = document.createElement("div"); title.className = "notes-title"; title.textContent = "Please Note"; wrapper.appendChild(title); notes.forEach( function (note) { const item = document.createElement("div"); item.className = "note"; item.textContent = "📌 " + note; wrapper.appendChild( item ); } ); container.appendChild( wrapper ); } /* ========================= WHATSAPP ========================= */ function getWhatsAppNumber() { if ( !businessData || !businessData.whatsapp ) { return ""; } return String( businessData.whatsapp.number || "" ).replace( /\D/g, "" ); } function createWhatsAppButton( message, className ) { const number = getWhatsAppNumber(); if (!number) { return null; } const button = document.createElement("a"); button.className = className || "product-order"; button.href = "https://wa.me/" + number + "?text=" + encodeURIComponent( message ); button.target = "_blank"; button.rel = "noopener noreferrer"; button.textContent = "WhatsApp"; return button; } /* ========================= GALLERY ========================= */ function renderGallery() { const container = $("galleryList"); container.innerHTML = ""; const gallery = businessData.gallery || []; if ( !Array.isArray(gallery) || gallery.length === 0 ) { return; } galleryImages = gallery.map( function (item) { if ( typeof item === "string" ) { return { src: resolvePath( item ), title: "" }; } return { src: resolvePath( item.image || item.src || item.path || item.url || "" ), title: item.title || "" }; } ).filter( function (item) { return item.src; } ); galleryImages.forEach( function (image, index) { const item = document.createElement( "div" ); item.className = "gallery-item"; const img = document.createElement( "img" ); img.src = image.src; img.alt = image.title || "Portrait Artwork"; img.loading = "lazy"; img.addEventListener( "click", function () { openGallery( index ); } ); item.appendChild(img); if (image.title) { const caption = document.createElement( "div" ); caption.className = "gallery-caption"; caption.textContent = image.title; item.appendChild( caption ); } container.appendChild( item ); } ); } /* ========================= LIGHTBOX ========================= */ function openGallery(index) { if ( galleryImages.length === 0 ) { return; } currentImage = index; updateLightbox(); $("lightbox") .classList .add("active"); } function updateLightbox() { const image = galleryImages[ currentImage ]; $("lightboxImage").src = image.src; $("lightboxCounter") .textContent = (currentImage + 1) + " / " + galleryImages.length; } function closeGallery() { $("lightbox") .classList .remove("active"); } function nextImage() { if ( galleryImages.length === 0 ) { return; } currentImage = (currentImage + 1) % galleryImages.length; updateLightbox(); } function previousImage() { if ( galleryImages.length === 0 ) { return; } currentImage = ( currentImage - 1 + galleryImages.length ) % galleryImages.length; updateLightbox(); } $("lightboxClose") .addEventListener( "click", closeGallery ); $("lightboxNext") .addEventListener( "click", nextImage ); $("lightboxPrev") .addEventListener( "click", previousImage ); $("lightbox") .addEventListener( "click", function (event) { if ( event.target === $("lightbox") ) { closeGallery(); } } ); document.addEventListener( "keydown", function (event) { if ( !$("lightbox") .classList .contains("active") ) { return; } if ( event.key === "Escape" ) { closeGallery(); } if ( event.key === "ArrowRight" ) { nextImage(); } if ( event.key === "ArrowLeft" ) { previousImage(); } } ); /* ========================= CONTACT ========================= */ function renderContact() { const contact = businessData.contact || {}; if (contact.phone) { $("phone").textContent = "Phone: " + contact.phone; } if (contact.location) { $("location").textContent = "Location: " + contact.location; } const number = getWhatsAppNumber(); const button = $("whatsappButton"); if (number) { button.href = "https://wa.me/" + number; button.style.display = "inline-block"; } else { button.style.display = "none"; } } /* ========================= SOCIAL ========================= */ function renderSocial() { const social = businessData.social || {}; setupSocial( "instagramButton", social.instagram ); setupSocial( "facebookButton", social.facebook ); } function setupSocial( id, url ) { const button = $(id); if (!url) { button.style.display = "none"; return; } button.href = url; button.style.display = "inline-block"; } }); 
