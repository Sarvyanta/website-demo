document.addEventListener("DOMContentLoaded", function () { const params = new URLSearchParams(window.location.search); const businessId = params.get("business") || "realisticportraitartist"; const businessFolder = "businesses/" + businessId + "/"; const businessConfigUrl = businessFolder + "config/data.json"; let currentBusinessData = null; let galleryImages = []; let currentImage = 0; /* ========================= HELPERS ========================= */ function getElement(id) { return document.getElementById(id); } function resolvePath(path) { if (!path) return ""; path = String(path); if ( path.startsWith("http://") || path.startsWith("https://") || path.startsWith("//") || path.startsWith("/") ) { return path; } return businessFolder + path; } function formatPrice(price) { if ( price === undefined || price === null || price === "" ) { return ""; } if (typeof price === "number") { return "₹" + price.toLocaleString("en-IN"); } return String(price); } /* ========================= WHATSAPP ========================= */ function getWhatsAppNumber(contact) { contact = contact || {}; let number = contact.whatsappNumber || contact.whatsapp || ""; if (typeof number === "object") { number = number.number || ""; } if (!number && currentBusinessData) { const business = currentBusinessData.business || currentBusinessData; number = business.whatsapp || business.whatsappNumber || ""; } if (!number && currentBusinessData) { const contactData = currentBusinessData.contact || {}; number = contactData.whatsapp || contactData.whatsappNumber || ""; } if (typeof number === "object") { number = number.number || ""; } return String(number) .replace(/\D/g, ""); } function createWhatsAppButton(message) { const number = getWhatsAppNumber(); if (!number) { return null; } const button = document.createElement("a"); button.className = "product-order"; button.href = "https://wa.me/" + number + "?text=" + encodeURIComponent(message); button.target = "_blank"; button.rel = "noopener noreferrer"; button.textContent = "WhatsApp"; return button; } /* ========================= LOAD JSON ========================= */ fetch(businessConfigUrl) .then(function (response) { if (!response.ok) { throw new Error( "Could not load " + businessConfigUrl ); } return response.json(); }) .then(function (data) { currentBusinessData = data; renderBusiness(data); }) .catch(function (error) { console.error(error); getElement("businessName").textContent = "Unable to load business"; getElement("tagline").textContent = ""; getElement("description").textContent = error.message; }); /* ========================= BUSINESS ========================= */ function renderBusiness(data) { const business = data.business || data; const sections = data.sections || {}; document.title = business.name || "Sarvyanta"; getElement("businessName").textContent = business.name || ""; getElement("tagline").textContent = business.tagline || ""; getElement("description").textContent = business.description || ""; if (business.logo) { const logo = getElement("businessLogo"); logo.src = resolvePath(business.logo); logo.style.display = "block"; } renderServices( sections.services || business.services || data.services || [] ); renderProducts( sections.products || business.products || data.products || [] ); renderNotes( sections.productsNotes || business.productsNotes || data.productsNotes || [] ); renderGallery( sections.gallery || business.gallery || data.gallery || [] ); renderContact( data.contact || business.contact || {} ); renderSocial( data.social || business.social || {} ); } /* ========================= SERVICES ========================= */ function renderServices(services) { const container = getElement("servicesList"); container.innerHTML = ""; if ( !Array.isArray(services) || services.length === 0 ) { return; } services.forEach(function (service) { const item = document.createElement("div"); item.className = "service-item"; let title = ""; let description = ""; if (typeof service === "string") { title = service; } else if (service) { title = service.name || service.title || service.service || "Service"; description = service.description || ""; } const heading = document.createElement("h3"); heading.textContent = title; item.appendChild(heading); if (description) { const text = document.createElement("p"); text.textContent = description; item.appendChild(text); } const whatsapp = createWhatsAppButton( "Hi, I am interested in your service: " + title ); if (whatsapp) { item.appendChild(whatsapp); } container.appendChild(item); }); } /* ========================= PRODUCTS ========================= */ function renderProducts(products) { const container = getElement("productsList"); container.innerHTML = ""; if ( !Array.isArray(products) || products.length === 0 ) { return; } products.forEach(function (product) { const card = document.createElement("div"); card.className = "product-card"; const title = document.createElement("h3"); title.textContent = product.name || product.title || "Product"; card.appendChild(title); if ( product.price !== undefined && product.price !== null ) { const price = document.createElement("div"); price.className = "product-price"; price.textContent = formatPrice(product.price); card.appendChild(price); } if (product.description) { const description = document.createElement("p"); description.textContent = product.description; card.appendChild(description); } const whatsapp = createWhatsAppButton( "Hi, I am interested in: " + (product.name || product.title || "this product") ); if (whatsapp) { card.appendChild(whatsapp); } container.appendChild(card); }); } /* ========================= NOTES ========================= */ function renderNotes(notes) { const container = getElement("productsNotes"); container.innerHTML = ""; if ( !Array.isArray(notes) || notes.length === 0 ) { return; } const wrapper = document.createElement("div"); wrapper.className = "notes"; notes.forEach(function (note) { const div = document.createElement("div"); div.className = "note"; div.textContent = "📌 " + note; wrapper.appendChild(div); }); container.appendChild(wrapper); } /* ========================= GALLERY ========================= */ function normalizeGalleryItem(item) { if (typeof item === "string") { return { src: resolvePath(item), title: "" }; } if (!item || typeof item !== "object") { return null; } const path = item.image || item.src || item.path || item.url; if (!path) { return null; } return { src: resolvePath(path), title: item.title || item.name || "" }; } function renderGallery(gallery) { const container = getElement("galleryList"); container.innerHTML = ""; galleryImages = []; if ( !Array.isArray(gallery) || gallery.length === 0 ) { return; } galleryImages = gallery .map(normalizeGalleryItem) .filter(Boolean); galleryImages.forEach( function (image, index) { createGalleryItem( image, index, container ); } ); } function createGalleryItem( image, index, container ) { const item = document.createElement("div"); item.className = "gallery-item"; const img = document.createElement("img"); img.src = image.src; img.alt = image.title || "Gallery Image"; img.loading = "lazy"; img.addEventListener( "click", function () { openGallery(index); } ); item.appendChild(img); if (image.title) { const caption = document.createElement("div"); caption.className = "gallery-caption"; caption.textContent = image.title; item.appendChild(caption); } container.appendChild(item); } /* ========================= LIGHTBOX ========================= */ function openGallery(index) { if (!galleryImages.length) { return; } currentImage = index; updateLightbox(); getElement("lightbox") .classList .add("active"); } function updateLightbox() { const image = galleryImages[currentImage]; getElement("lightboxImage").src = image.src; getElement("lightboxCounter").textContent = (currentImage + 1) + " / " + galleryImages.length; } function closeGallery() { getElement("lightbox") .classList .remove("active"); } function nextImage() { if (!galleryImages.length) { return; } currentImage = (currentImage + 1) % galleryImages.length; updateLightbox(); } function previousImage() { if (!galleryImages.length) { return; } currentImage = (currentImage - 1 + galleryImages.length) % galleryImages.length; updateLightbox(); } getElement("lightboxClose") .addEventListener( "click", closeGallery ); getElement("lightboxNext") .addEventListener( "click", nextImage ); getElement("lightboxPrev") .addEventListener( "click", previousImage ); getElement("lightbox") .addEventListener( "click", function (event) { if ( event.target === getElement("lightbox") ) { closeGallery(); } } ); document.addEventListener( "keydown", function (event) { const lightbox = getElement("lightbox"); if ( !lightbox.classList.contains("active") ) { return; } if (event.key === "Escape") { closeGallery(); } if (event.key === "ArrowRight") { nextImage(); } if (event.key === "ArrowLeft") { previousImage(); } } ); /* ========================= CONTACT ========================= */ function renderContact(contact) { if (contact.phone) { getElement("phone").textContent = "Phone: " + contact.phone; } if (contact.location) { getElement("location").textContent = "Location: " + contact.location; } const number = getWhatsAppNumber(contact); const button = getElement("whatsappButton"); if (number) { button.href = "https://wa.me/" + number; button.style.display = "inline-block"; } else { button.style.display = "none"; } } /* ========================= SOCIAL ========================= */ function renderSocial(social) { setupSocialButton( "instagramButton", social.instagram ); setupSocialButton( "facebookButton", social.facebook ); } function setupSocialButton( id, url ) { const button = getElement(id); if (!url) { button.style.display = "none"; return; } button.href = url; button.style.display = "inline-block"; } }); 
